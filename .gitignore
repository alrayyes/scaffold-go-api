# What this scaffold builds, not what an editor or OS drops beside it —
# those live in ~/.config/git/gitignore_global, not here.

/scaffold-go-api
/dist
coverage.out
junit.xml

node_modules/
.bun-cache/

# Written by the release job, not by hand.
CHANGELOG.md

# Vale's style packages, downloaded by `vale sync`.
/styles/Google/
/styles/proselint/

# Staged by the pages workflow from api/openapi.yaml right before
# actions/upload-pages-artifact runs. Not committed — it's a copy, not a
# second source of truth.
/docs/openapi.yaml
